export type TransactionRequest = {
  usersId: string;
  propertiesId: string;
  finalPrice: number;
  status: string;
  paymentMethod: string;
  firstName: string;
  lastName: string;
  mobileNumber: string;
};
