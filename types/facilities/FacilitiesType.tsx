export type FacilitiesType = {
    id: string;
    name: string;
    // logoUrl?: string;
    // createdAt: string;  // Since `createdAt` is an Instant in the backend, it would be a string in TypeScript.
    // updatedAt: string;
    // deletedAt?: string;  // Optional since it could be null.
};